/**
 * @format
 */

import {AppRegistry} from 'react-native';
import TestScreen from './src/compons/TestScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => TestScreen);
